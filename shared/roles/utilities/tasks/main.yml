---
# Utilities role: Keka (macOS) + archive tools (Linux)

- name: Install Keka
  ansible.builtin.include_tasks: keka.yml
  tags: [keka]

- name: Install archive tools
  ansible.builtin.include_tasks: archive-tools.yml
  tags: [archive-tools]
