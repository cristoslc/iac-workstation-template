---
# Archive tools sub-task (Debian only)
- name: Install archive utilities (Debian)
  when: ansible_facts.os_family == "Debian"
  ansible.builtin.apt:
    name:
      - p7zip-full
      - unrar
    state: present
  become: true
