# Managed by iac-daily-driver-environments.
# Machine-specific SSH config goes in ~/.ssh/config.d/local

# Include platform-specific and local overrides
Include config.d/*

Host *
	IdentityAgent ~/.1password/agent.sock
